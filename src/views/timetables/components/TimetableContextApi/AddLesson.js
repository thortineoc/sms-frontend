import React, {useContext} from 'react';
import './TimetableContext';

const AddLesson = () => {
    const [classes, setClasses] = useContext(TimetableContext);

    return (
        <div>

        </div>
    );
};

export default AddLesson;